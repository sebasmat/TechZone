// const { Users, Products, Products } = require("../db");

// const actualizarStock = async (productId, stock) => {
//   await Products.update(
//     {stock: stock },
//     { where: {ProductId: productId } }
//   );
 
// };

// module.exports = actualizarStock;